import { Input } from "./ui/input";

export const renderFields = (fieldNames: string[]) => {
  return fieldNames.map((fieldName) => (
    <div key={fieldName}>
        <h1>hello</h1>
    </div>
  ));
};
